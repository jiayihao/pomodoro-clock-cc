# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This directory currently contains no application source files, README, package manifests, test configuration, Cursor rules, or Copilot instructions. The only discovered project file is `.claude/settings.local.json`, which contains local Claude Code permission settings.

## Commands

No build, lint, test, or development commands are defined yet. Before adding command guidance here, inspect newly added project files such as `README.md`, `package.json`, `Makefile`, `pyproject.toml`, `Cargo.toml`, or framework-specific config files.

## Architecture

No code architecture can be documented yet because the repository has no source tree at this time. Update this section after the project’s implementation files are added.
